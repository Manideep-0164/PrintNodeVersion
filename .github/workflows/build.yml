on: push
jobs:
    first-job:
            runs-on: windows-latest
            steps:
                - run: node --version
    second-job:
            runs-on: windows-latest
            steps:
                - run: npm --version